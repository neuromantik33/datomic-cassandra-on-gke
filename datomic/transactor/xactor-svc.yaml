apiVersion: v1
kind: Service
metadata:
  labels:
    app: datomic
    role: transactor
  name: transactor
  namespace: datomic
spec:
  externalTrafficPolicy: Cluster
  loadBalancerIP: "" # {"$kpt-set":"transactor-ip"}
  loadBalancerSourceRanges: # {"$kpt-set":"authorized-networks"}
  - "0.0.0.0/0"
  ports:
  - name: datomic
    port: 4334
    protocol: TCP
    targetPort: datomic
  selector:
    app: datomic
    role: transactor
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
  type: LoadBalancer
